---
import Layout from "../layouts/Layout.astro";
import { BASE, ORIGINAL_SITE_URL } from "../consts";

const releases = [
  {
    version: "26.02",
    date: "2026-06-25",
    note: "Latest stable release.",
  },
  {
    version: "26.01",
    date: "2026-04-02",
    note: "Maintenance release.",
  },
  {
    version: "26.00",
    date: "2026-01-18",
    note: "Feature release.",
  },
];
---

<Layout
  title="7-Zip — a free file archiver with a high compression ratio"
  description="7-Zip is a free, open-source file archiver with a high compression ratio, built around the 7z format and LZMA/LZMA2 compression."
>
  <section class="hero">
    <p class="eyebrow">Free · open-source · no ads, no bundleware</p>
    <h1>A file archiver that gets out of your way</h1>
    <p class="lede">
      7-Zip compresses harder than ZIP, opens almost anything you throw at it,
      and costs nothing — for personal use or in a business, with no
      registration required.
    </p>
    <div class="button-row">
      <a class="button primary" href="https://github.com/ip7z/7zip/releases/download/26.02/7z2602-x64.exe">
        Download for Windows <span class="sub">x64 · 26.02</span>
      </a>
      <a class="button secondary" href={`${BASE}download/`}> 32-bit &amp; ARM64 </a>
    </div>
    <p class="lede" style="margin-top: 1rem; font-size: 0.95rem;">
      Need Linux, macOS, source, or an older release? See <a href={`${BASE}download/`}>all downloads</a>.
    </p>
  </section>

  <section>
    <h2>Why it's worth installing</h2>
    <div class="grid">
      <div class="card">
        <h3>High compression</h3>
        <p>
          The native <code>7z</code> format, built on LZMA and LZMA2, typically
          compresses tighter than ZIP — usually 30–70% smaller depending on the
          data.
        </p>
      </div>
      <div class="card">
        <h3>Opens almost anything</h3>
        <p>Packs and unpacks 7z, XZ, BZIP2, GZIP, TAR, ZIP, and WIM.</p>
        <p>
          Unpacks-only: APFS, AR, ARJ, CAB, CHM, CPIO, CramFS, DMG, EXT, FAT,
          GPT, HFS, IHEX, ISO, LZH, LZMA, MBR, MSI, NSIS, NTFS, QCOW2, RAR,
          RPM, SquashFS, UDF, UEFI, VDI, VHD, VMDK, XAR, and Z.
        </p>
      </div>
      <div class="card">
        <h3>Security built in</h3>
        <p>
          AES-256 encryption for 7z and ZIP archives, plus support for
          self-extracting archives when you need to send one to someone
          without 7-Zip installed.
        </p>
      </div>
      <div class="card">
        <h3>Fits how you already work</h3>
        <ul>
          <li>Windows Shell integration (right-click to zip/unzip)</li>
          <li>Standalone file manager</li>
          <li>Command-line version for scripts and automation</li>
          <li>FAR Manager plugin</li>
        </ul>
      </div>
      <div class="card">
        <h3>Works everywhere you need it</h3>
        <p>
          Windows XP through 11, plus Server 2003–2025 (32-bit, 64-bit, and
          ARM64). Available in 87 languages.
        </p>
      </div>
      <div class="card">
        <h3>Actually free</h3>
        <p>
          Mostly GNU LGPL, with some parts under a BSD 3-clause license and an
          unRAR license restriction on a small piece of the code. No
          registration, no payment, commercial use included.
        </p>
      </div>
    </div>
  </section>

  <section>
    <h2>Recent releases</h2>
    <ul class="timeline">
      {
        releases.map((release) => (
          <li>
            <time datetime={release.date}>{release.date}</time>
            <strong>7-Zip {release.version}</strong> — {release.note}
          </li>
        ))
      }
    </ul>
    <p>
      <a href="https://www.7-zip.org/history.txt">Full version history →</a>
    </p>
  </section>

  <section class="panel">
    <h2>About this redesign</h2>
    <p>
      This is a rebuild of <a href={ORIGINAL_SITE_URL}>7-zip.org</a>, the real
      site for the real project — done as a studio exercise, not something
      the 7-Zip team asked for.
    </p>
    <p>
      I like 7-Zip because it's exactly what it says it is: a free, genuinely
      excellent archiver with no dark patterns and no upsell. What its current
      site gets wrong is presentation, not substance — the download links sit
      in a small table with no visual priority over anything else on the
      page, the feature list is one long unbroken paragraph instead of
      something scannable, and every release since 2010 is stacked on a single
      page with no way to see "what's current" at a glance. None of that is a
      content problem; it's an information-hierarchy problem. This version
      keeps every real fact — the licenses, the format list, the platform
      support — and gives the one thing most visitors came for, the download,
      an actual visual priority.
    </p>
  </section>
</Layout>
